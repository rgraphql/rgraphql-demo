apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: rgraphql-demo
    tier: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: rgraphql-demo
    tier: frontend
